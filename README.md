# bcnoteTaker

This is an application designed to allow the user to take notes, which are then stored through a JSON file.
The user can also delete notes that aren't needed.


Deployed Heroku application:
https://floating-tundra-43330.herokuapp.com/
